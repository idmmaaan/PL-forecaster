"""Training and promotion entry points, all CLI-only.

Submodules are imported directly rather than re-exported here, so that
`python -m epl_predictor.training.baselines` does not import it twice.
"""
